// Placeholder de checagem de pré-requisitos.
export async function prerequisitesMet(_userId:string, _preReqs:string[]): Promise<boolean> {
  // TODO: integrar com progress-service para validar cursos concluídos.
  return true;
}
